import {ExpandableCard} from "../components/expandable-card.tsx";

const SectionProjects = () => {
    return (
        <section id="section-projects" className="space-y-6">
            <h3 className="text-lg font-medium">Projects</h3>

            <ExpandableCard/>
        </section>
    );
};

export default SectionProjects;